% @CLASSIFIERENSEMBLE
%
% Files
%   classifierensemble - function model=classifierensemble(varargin)
%   display            - display(x)
%   get                - function value = get(model, param)
%   set                - function model = set(model, param, value)
%   train              - function [model, trainerr] = train(model, urbilder, bilder, sampleclass, trainparams, eps, varargin)
